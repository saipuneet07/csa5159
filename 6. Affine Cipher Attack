from collections import Counter
c=input().upper()
f=[x[0]for x in Counter(c).most_common(2)]
print("Most:",f[0],"Second:",f[1])
for a in range(1,26):
 if __import__("math").gcd(a,26)==1:
  for b in range(26):
   r=""
   ai=pow(a,-1,26)
   for i in c:
    if i.isalpha():
     r+=chr((ai*((ord(i)-65)-b)%26)+65)
    else:r+=i
   print("a=",a,"b=",b,r)
